# Project Rules Local (Project-Owned)

This file is project-owned.
Use it for project-specific additions or overrides to `project.rules.base.md`.

Guidelines:
- Keep only project-specific deltas.
- Do not duplicate sections already covered in `project.rules.base.md`.
- When an item conflicts with base rules, write the override explicitly and explain why.

## Local Additions / Overrides

- [Add project-specific rule here]
